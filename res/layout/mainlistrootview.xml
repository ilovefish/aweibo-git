<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/mainListRootView"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:background="@color/mi_se_center"
        android:paddingTop="8dip"
    	android:paddingBottom="10dip"
    	android:clickable="true"
    	android:focusable="true"
    	>
    <TextView 
        android:id="@+id/mainListRootView_TextView "
        android:layout_width="wrap_content"
    	android:layout_height="wrap_content"
    	android:visibility="gone"
        android:text="添加"
        android:textAppearance="@android:style/TextAppearance.Theme"
        android:layout_gravity="center_vertical|center_horizontal"
        android:textColor="@color/black_end"
        />
    <ProgressBar
    	android:id="@+id/mainListRootView_ProgressBar"
    	android:visibility="visible"
    	android:layout_width="50dp"
    	android:layout_height="50dp"
    	android:layout_gravity="center_vertical|center_horizontal"
        android:indeterminate="false"></ProgressBar>
</FrameLayout>